(()=>{const form=document.querySelector('#registrationForm'),button=form.querySelector('button'),error=document.querySelector('#registrationError'),success=document.querySelector('#registrationSuccess');form.addEventListener('submit',async event=>{event.preventDefault();button.disabled=true;button.textContent='Submitting…';error.textContent='';const data=new FormData(form);try{const response=await fetch('/api/v1/public/client-registrations',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({organizationName:data.get('organizationName'),contactName:data.get('contactName'),email:data.get('email'),country:data.get('country'),infrastructureMode:data.get('infrastructureMode'),website:data.get('website'),consent:data.get('consent')==='on'})}),payload=await response.json();if(!response.ok)throw Error(payload.error||'Registration could not be submitted');form.hidden=true;success.hidden=false;success.focus()}catch(problem){error.textContent=problem.message||'Registration could not be submitted';button.disabled=false;button.textContent='Submit registration'}})})();
